important: sudo chmod 0666 /dev/uinput

todo:
  - better mouse tracking (it's still bit buggy)
    - it gets stuck at 0,0 and maxX or maxY + when initial touching it gets teleported kind of thing.
  - better left click + add right click + hold click of left click
  - keyboard support (should be easy i think)
